# RFID_Based_ACCESS_GATEWAY
The system functions as an automated entry point where students present pre-assigned RFID cards. Upon successful authentication, the gateway provides real-time sensory feedback and redirects the user to a Google Form to record their attendance with precise timestamps.
